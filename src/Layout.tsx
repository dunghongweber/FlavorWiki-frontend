import React from "react";
/**
 *  DUMMY FILE
 * this could be used if the app is bigger and need specific layout for front end
 * @returns DUMMY FILE
 */
export default function Layout() {
  return <div>Layout</div>;
}
